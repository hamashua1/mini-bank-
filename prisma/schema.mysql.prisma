generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/.prisma/client-mysql"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL_MYSQL")
}

// User and auth models are intentionally absent — auth is handled by MongoDB exclusively.
// userId on Wallet is a plain string reference to the MongoDB user _id.

model Wallet {
  id           String        @id @default(uuid()) @db.Char(36)
  userId       String        @unique @db.VarChar(36)
  balance      Decimal       @default(0) @db.Decimal(19, 4)
  currency     String        @db.VarChar(3)
  createdAt    DateTime      @default(now())
  transactions Transaction[]
}

model Transaction {
  id            String          @id @default(uuid()) @db.Char(36)
  walletId      String          @db.Char(36)
  type          TransactionType
  amount        Decimal         @db.Decimal(19, 4)
  balanceBefore Decimal         @db.Decimal(19, 4)
  balanceAfter  Decimal         @db.Decimal(19, 4)
  description   String          @db.VarChar(200)
  createdAt     DateTime        @default(now())
  wallet        Wallet          @relation(fields: [walletId], references: [id], onDelete: Cascade)
}

enum TransactionType {
  deposit
  withdraw
  transfer_in
  transfer_out
}
